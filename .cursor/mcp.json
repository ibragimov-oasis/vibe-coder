{
  "mcpServers": {
    "lightpanda": {
      "command": "npx",
      "args": ["-y", "lightpanda-mcp"],
      "description": "MANDATORY browser for all web tasks. 9x faster than Chrome, 16x less memory. NEVER use Chrome."
    },
    "gitnexus": {
      "command": "npx",
      "args": ["-y", "gitnexus@latest", "mcp"],
      "description": "Codebase map and analysis. Use to understand project structure before coding."
    },
    "supermemory": {
      "url": "https://mcp.supermemory.ai/mcp",
      "description": "Long-term memory across sessions. Check before every task, save learnings after."
    },
    "openviking": {
      "command": "npx",
      "args": ["-y", "@openviking/mcp"],
      "description": "Codebase context memory. Stores what was changed and why."
    },
    "nano-banana": {
      "command": "npx",
      "args": ["-y", "nano-banana-2-mcp"],
      "description": "Image generation via Gemini. Use for UI mockups, icons, brand assets.",
      "env": {
        "GEMINI_API_KEY": "${GEMINI_API_KEY}"
      }
    },
    "mcp-toolbox": {
      "command": "npx",
      "args": ["-y", "@toolbox-sdk/server", "--prebuilt=postgres"],
      "description": "Database access MCP — PostgreSQL, MySQL, BigQuery, MongoDB, Redis, 20+ databases."
    },
    "markitdown": {
      "command": "markitdown",
      "args": [],
      "description": "Convert files to Markdown — PDF, DOCX, XLSX, images, audio, HTML for LLM consumption."
    },
    "code-review-graph": {
      "command": "uv",
      "args": ["run", "code-review-graph", "serve"],
      "description": "Tree-sitter AST code graph — 8.2x token reduction, blast-radius analysis, 19 languages."
    },
    "task-master-ai": {
      "command": "npx",
      "args": ["-y", "task-master-ai"],
      "env": {
        "TASK_MASTER_TOOLS": "all"
      },
      "description": "AI task management — PRD→tasks→dependencies. 36 tools. Complexity analysis."
    }
  }
}
